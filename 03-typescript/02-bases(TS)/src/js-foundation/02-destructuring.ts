// console.log( process.env );

const { SHELL, WINDIR, npm_config_sso_poll_frequency } = process.env;

// console.table({SHELL, WINDIR, npm_config_sso_poll_frequency});


const characters = ['Flash', 'Superman', 'Green Lantern', 'Batman'];

const [, , , batman] = characters;

// console.log(batman);

